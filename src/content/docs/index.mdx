---
title: OAShield
description: Turn your OpenAPI spec into a Web Application Firewall that only allows valid API calls.
template: splash
hero:
  title: Your API spec is already a security policy.
  tagline: OAShield turns your OpenAPI specification into WAF rules that permit exactly the requests your API defines, and deny everything else. Free, open source, and deployable on ModSecurity or Coraza.
  image:
    file: ../../assets/oashield.webp
  actions:
    - text: Get Started
      link: /guides/install
      icon: right-arrow
    - text: How It Works
      link: /how
      icon: open-book
      variant: minimal
    - text: View on GitHub
      link: https://github.com/cognitivegears/oashield
      icon: github
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## One spec. One command. A positive security model.

A traditional WAF is a blocklist. It tries to recognize known-bad requests, and anything
it hasn't seen before gets through. OAShield flips that around: your OpenAPI spec defines
what's **valid**, and the generated rules deny everything else by default.

```bash
java -cp oashield-cli.jar org.openapitools.codegen.OpenAPIGenerator \
  generate -g modsecurity3 -i your-api.yaml -o rules/
```

The output is standard [SecLang](https://coraza.io/docs/seclang/). Deploy it to
[OWASP ModSecurity v3](https://github.com/owasp-modsecurity/ModSecurity) or
[Coraza](https://coraza.io) alongside your API, in a sidecar, or at your edge.

## What the generated rules enforce

<CardGrid stagger>
	<Card title="Deny by default" icon="warning">
		Endpoints, methods, and parameters not declared in your spec are rejected. As far
		as an attacker can tell, undocumented and "shadow" API surfaces don't exist.
		Fails closed, not open.
	</Card>
	<Card title="Full parameter validation" icon="approve-check">
		Path, query, and form parameters are checked against your schema: types, formats,
		enums, patterns, numeric bounds, lengths, and cardinality. Undeclared parameters
		are blocked.
	</Card>
	<Card title="Request-body validation" icon="document">
		JSON bodies are validated field by field: required properties, types, bounds, and
		an allowlist that rejects undeclared fields. On Coraza, the raw body is also
		validated against a generated JSON Schema.
	</Card>
	<Card title="Two engines, one option" icon="setting">
		Target ModSecurity v3 (libmodsecurity) or Coraza with a single flag. Most rules are
		identical across both, and OAShield handles the engine-specific differences for you.
	</Card>
	<Card title="Configurable blocking" icon="random">
		Deny with any status code, drop, redirect, or run in detection-only mode that logs
		contract violations without blocking anything. Handy for rolling out safely.
	</Card>
	<Card title="Built for CI/CD" icon="rocket">
		Regenerate rules on every release so your WAF always matches your API. No drift,
		no manual rule writing. Pairs well with the
		[OWASP Core Rule Set](https://coreruleset.org/) for defense in depth.
	</Card>
</CardGrid>

## See it in action

<CardGrid>
	<Card title="Quick Start" icon="rocket">
		Generate and deploy your first rule set in minutes.
		[Get started →](/guides/start)
	</Card>
	<Card title="How It Works" icon="open-book">
		Walk through the generated rules, block by block.
		[Read the walkthrough →](/how)
	</Card>
	<Card title="Free & open source" icon="github">
		Apache 2.0 licensed. Issues and pull requests welcome.
		[View the license →](/license)
	</Card>
</CardGrid>
